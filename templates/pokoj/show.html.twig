{% extends 'base.html.twig' %}

{% block title %}Pokój{% endblock %}

{% block body %}
    <h1>Pokój</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pokoj.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ pokoj.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-warning" href="{{ path('pokoj_index') }}">Powrót do listy</a>

    <a class = "btn btn-sm btn-success" href="{{ path('pokoj_edit', {'id': pokoj.id}) }}">Edytuj</a>

    {{ include('pokoj/_delete_form.html.twig') }}
{% endblock %}
