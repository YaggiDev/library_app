{% extends 'base.html.twig' %}

{% block title %}Kategoria index{% endblock %}

{% block body %}
    <h1>Kategoria index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for kategorium in kategorias %}
            <tr>
                <td>{{ kategorium.id }}</td>
                <td>
                    <a href="{{ path('kategoria_show', {'id': kategorium.id}) }}">show</a>
                    <a href="{{ path('kategoria_edit', {'id': kategorium.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('kategoria_new') }}">Create new</a>
{% endblock %}
