{% extends 'base.html.twig' %}

{% block title %}Kategoria index{% endblock %}

{% block body %}
    <h1>Kategoria index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nazwa kategorii</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for kategoria in kategorie %}
            <tr>
                <td>{{ kategoria.id }}</td>
                <td>{{ kategoria.nazwa }}</td>
                <td>
                    <a href="{{ path('kategoria_show', {'id': kategoria.id}) }}">show</a>
                    <a href="{{ path('kategoria_edit', {'id': kategoria.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('kategoria_new') }}">Create new</a>
{% endblock %}
